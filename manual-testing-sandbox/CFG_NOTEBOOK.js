import * as PCL from "system/pcl"; // include 3PQ Configuration Library

// called on configuration change until fixed point is found
globalThis.OnChangeConfiguration = () => {
  // set a selected value of an attribute
  




};
